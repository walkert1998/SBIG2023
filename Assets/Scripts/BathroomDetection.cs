using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BathroomDetection : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // public int CheckTriggerType(Collider other)
    // {
    //     if (FactionSystemManager.GetFactionOpinion(Factions.Player, Factions.VioletCadaver) > 10)
    //     {
    //         return 1;
    //     }
    //     else if (FactionSystemManager.GetFactionOpinion(Factions.Player, Factions.VioletCadaver) < 0 && )
    //     {
    //         return 2;
    //     }
    //     return 0;
    // }
}
